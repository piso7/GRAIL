accelerate==0.29.3
datasets==2.17.1
evaluate==0.4.2
numpy==1.26.2
pandas==2.2.1
peft==0.10.0
rouge_score==0.1.2
scikit_learn==1.1.3
scipy==1.10.1
sentencepiece
tqdm==4.65.0
transformers==4.37.0
wandb==0.17.5

# Optional GPU-only accelerators (require CUDA + compilation; not needed for the
# core pipeline). Install manually if you want them:
#   deepspeed==0.14.4
#   flash_attn==2.5.8
#
# Install torch separately, matching your CUDA/CPU platform, e.g.:
#   pip install torch==2.1.2
